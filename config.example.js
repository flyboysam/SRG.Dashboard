// Copy to config.js and add your Adafruit IO credentials.
// config.js is gitignored — your key will never be committed to GitHub.
//
// For local development: copy this file to config.js and fill in values.
// For GitHub Pages: add repo secrets AIO_Username and AIO_Key — the deploy
// workflow (scripts/build-config.js) generates config.js at build time.
//
window.ADAFRUIT_IO_USERNAME = 'sbevans';
window.ADAFRUIT_IO_KEY = '';
// Optional: override feed names if yours differ (both temps in °C)
// window.ADAFRUIT_MS5611_TEMP_FEED = 'temperature';  // MS5611 barometric sensor
// window.ADAFRUIT_PI_CORE_TEMP_FEED = 'gpu-temp';   // Pi-Core temp
